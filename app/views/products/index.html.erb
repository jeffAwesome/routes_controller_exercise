<% first_product = Product.first %>
<% product = Product.where(:name => 'rails book').first %>

<% cheap_product = Product.where('price > 1').first %>

<p>
	Product Name: "<%= first_product.name %>""
	costs $<%= first_product.price %>
</p>
<p>
	The seller for this product is 
	<%= first_product.user.name %>
</p>

<p>
	Product Name: "<%= cheap_product.name %>"
</p>
<h2>Hello World</h2>
<h2> I AMA View </h2>
<p>
  Find me in <%= Rails.root.join("app", "views", "products", __FILE__ ) %>
</p>
